# firebase-service
Roblox module that enables server-side communication with Firebase Realtime Database through Google Apps Script authentication, supporting CRUD operations (create, read, update, delete) with query parameters and automatic token refresh
